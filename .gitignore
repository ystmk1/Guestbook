node_modules
dist
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel

# 원본 스캔 (29MB) — public/model.glb 로 압축해서 배포한다
*.glb
!public/model.glb

# 원본 영상 (40MB) — public/bg.mp4 로 압축해서 배포한다
*.mp4
!public/bg.mp4

# 원본 폰트 (상용) — public/fonts/ 의 서브셋만 배포한다
*.otf
*.ttf

# 원본 사진 (장당 10MB 안팎) — public/img/ 의 압축본만 배포한다
image*.png

# 프로젝트 뿌리의 원본 이미지 — public/img/ 의 압축본만 배포한다
/*.png
