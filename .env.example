# =====================================================================
#  이 파일을 .env 로 복사해서 쓴다. .env 는 git 에 올라가지 않는다.
#  아무것도 설정하지 않아도 앱은 "로컬 모드"로 정상 동작한다.
# =====================================================================

# ---------------------------------------------------------------------
# Supabase — 넣으면 서버 모드로 자동 전환, 비워두면 로컬(localStorage) 모드
# Supabase Dashboard > Project Settings > API 에서 복사
# ---------------------------------------------------------------------
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ---------------------------------------------------------------------
# 화면 좌상단에 뜨는 전시 이름
# ---------------------------------------------------------------------
VITE_EXHIBITION_TITLE=SYMPOIESIS

# ---------------------------------------------------------------------
# 관리자 숨김(Ctrl+Shift+H) PIN — 로컬 모드에서만 쓴다.
# 서버 모드에서는 supabase/schema.sql 의 admin_config 에 넣은 PIN 을 쓴다.
# ---------------------------------------------------------------------
VITE_ADMIN_PIN=1234
