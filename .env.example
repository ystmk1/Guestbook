# =====================================================================
#  이 파일을 .env 로 복사해서 쓴다. .env 는 git 에 올라가지 않는다.
#  아무것도 설정하지 않으면 이 브라우저의 localStorage 에 저장된다.
# =====================================================================

# Supabase — 넣으면 서버 모드, 비워두면 로컬 모드
# Supabase Dashboard > Project Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
